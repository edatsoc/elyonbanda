<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sorteio Banda El-Elyon</title>
    <style>
        /* Estilização Geral */
        body {
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: white;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            /* Fundo Verde Gradiente com efeito de 'pincelada' usando radial gradients */
            background: radial-gradient(circle at 20% 30%, rgba(255, 255, 255, 0.1) 0%, transparent 20%),
                        radial-gradient(circle at 80% 70%, rgba(255, 255, 255, 0.1) 0%, transparent 25%),
                        linear-gradient(135deg, #2ecc71 0%, #27ae60 50%, #1b4d3e 100%);
            background-attachment: fixed;
        }

        .container {
            background: rgba(0, 0, 0, 0.2);
            backdrop-filter: blur(10px);
            padding: 40px;
            border-radius: 20px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
            max-width: 500px;
            width: 90%;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        h1 {
            font-size: 3rem;
            margin-bottom: 0;
            letter-spacing: 4px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
        }

        .banda-nome {
            font-size: 1.5rem;
            font-weight: 300;
            margin-top: 5px;
            opacity: 0.9;
        }

        .prize-img {
            width: 100%;
            max-width: 250px;
            height: auto;
            margin: 30px 0;
            border-radius: 15px;
            filter: drop-shadow(0 5px 15px rgba(0,0,0,0.2));
        }

        .info-box {
            background: rgba(255, 255, 255, 0.1);
            padding: 20px;
            border-radius: 10px;
            margin: 20px 0;
        }

        .price {
            font-size: 2rem;
            font-weight: bold;
            color: #f1c40f; /* Destaque em dourado */
        }

        .date {
            font-size: 1.1rem;
            margin-bottom: 10px;
        }

        .link-regulamento {
            color: #fff;
            text-decoration: underline;
            font-size: 0.9rem;
            opacity: 0.8;
            transition: 0.3s;
        }

        .link-regulamento:hover {
            opacity: 1;
            color: #f1c40f;
        }

        /* Botão fictício ou de ação */
        .btn-acao {
            display: inline-block;
            margin-top: 20px;
            padding: 12px 30px;
            background-color: #fff;
            color: #27ae60;
            text-decoration: none;
            border-radius: 50px;
            font-weight: bold;
            transition: transform 0.2s;
        }

        .btn-acao:hover {
            transform: scale(1.05);
        }
    </style>
</head>
<body>

    <div class="container">
        <header>
            <h1>SORTEIO</h1>
            <div class="banda-nome">Banda El-Elyon</div>
        </header>

        <img src="https://images.unsplash.com/photo-1618941716939-553f3609347d?q=80&w=500&auto=format&fit=crop" alt="Ventilador de Mesa" class="prize-img">

        <div class="info-box">
            <p class="date">📅 Data Prevista: <strong>07 de Março de 2026</strong></p>
            <p>Prêmio: <strong>Um Ventilador de Mesa</strong></p>
            <p class="price">R$ 5,00</p>
        </div>

        <a href="#" class="btn-acao">Quero participar</a>

        <div style="margin-top: 25px;">
            <a href="regulamento.pdf" class="link-regulamento">Acesse aqui o regulamento do sorteio</a>
        </div>
    </div>

</body>
</html>
